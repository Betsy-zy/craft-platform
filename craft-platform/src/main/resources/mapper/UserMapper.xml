<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.craft.mapper.UserMapper">
    <resultMap id="UserMap" type="com.craft.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="avatar" property="avatar"/>
    </resultMap>

    <insert id="insert" parameterType="com.craft.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, phone, role, status, create_time, avatar)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{role}, #{status}, #{createTime}, #{avatar})
    </insert>

    <select id="findById" parameterType="java.lang.Long" resultMap="UserMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findByUsername" parameterType="java.lang.String" resultMap="UserMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findByEmail" parameterType="java.lang.String" resultMap="UserMap">
        SELECT * FROM user WHERE email = #{email}
    </select>

    <select id="findAll" resultMap="UserMap">
        SELECT * FROM user
    </select>

    <update id="update" parameterType="com.craft.entity.User">
        UPDATE user
        SET username = #{username},
            password = #{password},
            email = #{email},
            phone = #{phone},
            role = #{role},
            status = #{status},
            avatar = #{avatar}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>